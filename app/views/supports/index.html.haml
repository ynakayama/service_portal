- title 'サポートメニュー管理 - イノベーション推進部サービスポータル'

.page-header
  %h2 サポートメニュー管理 - 一覧画面
.supports-index
  .container
    .content
      .row
        .span3
          = link_to 'サービス管理に戻る', services_path, :class => 'btn btn-small'

        .span9
          %h2 サポートメニュー一覧
          %table{:class => 'table table-bordered table-striped'}
            %thead
              %tr
                %th
                  ID
                %th
                  サポートメニュー
                %th
                  説明文
                %th
                  対象サービス
                %th
                  処理
            %tbody
              - @supports.each do |support|
                %tr
                  %td
                    = link_to support.id, edit_support_path(support)
                  %td
                    = support.course
                  %td
                    = support.text
                  %td
                    = support.service_type
                  %td
                    = link_to '詳細', support
                    = link_to '編集', edit_support_path(support)
                    = link_to '削除', support, :confirm => '本当に削除しますか', :method => :delete
          .form-actions
            = link_to '新規追加', new_support_path, :class => 'btn btn-primary'

          = paginate @supports, :window => 4, :outer_window => 0
